services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: landing-page
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgresql://neondb_owner:npg_afJ03pjMxtYU@ep-weathered-cloud-afm3pr6r-pooler.c-2.us-west-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require
      - NODE_ENV=production
    restart: always
